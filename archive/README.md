# Archived legacy project files

These files are preserved for recovery and historical reference. They are not
served by Express and are excluded from deployment. Do not run or deploy the
legacy account handlers, old pages, or old build output.

The current website lives in `site/`. The root README describes the active app.
The separate Python lead-research tools remain active outside this archive.
